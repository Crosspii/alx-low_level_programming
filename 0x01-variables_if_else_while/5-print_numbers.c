#include <stdio.h>
/* main - prints alld the numbers from 0 to 9
 * return: is always 0
 */

int main(void)
{
  int num;
  for (num = 0; num < 10; num++)
    {
      puts(num);
    }
  putchar('\n');
  return(0);
}
