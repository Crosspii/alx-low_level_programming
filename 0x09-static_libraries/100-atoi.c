#include "main.h"
#include <stdio.h>

/**
 * _atoi - converts a string to an integer
 * @s: pointer to the string to be converted
 * Return: integers converted
 */
int _atoi(char *s)
{
	return (*s);
}
